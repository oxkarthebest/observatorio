var size = 0;
var placement = 'point';

var style_U_PERIMETRO_3 = function(feature, resolution){
    var context = {
        feature: feature,
        variables: {}
    };
    var value = ""
    var labelText = "";
    size = 0;
    var labelFont = "16.900000000000002px \'Corbel\', sans-serif";
    var labelFill = "#323232";
    var bufferColor = "#fafafa";
    var bufferWidth = 3.0;
    var textAlign = "left";
    var offsetX = 8;
    var offsetY = 3;
    var placement = 'point';

    let cadena = String(feature.get("NOMBRE_GEO"))
    const primerCaracter = cadena.charAt(0);
    const restoDeLaCadena = cadena.substring(1, cadena.length).toLowerCase();
    if ((cadena) !== null) {
        labelText = primerCaracter.concat(restoDeLaCadena);
    }
    
    var style = [ new ol.style.Style({
        stroke: new ol.style.Stroke({color: 'rgba(35,35,35,1.0)', lineDash: null, lineCap: 'butt', lineJoin: 'miter', width: 2}),fill: new ol.style.Fill({color: 'rgba(190,207,80,0.0)'}),
        text: createTextStyle(feature, resolution, labelText, labelFont,
                              labelFill, placement, bufferColor,
                              bufferWidth)
    })];

    return style;
};
